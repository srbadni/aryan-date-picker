import { defaultCalendarAdapter } from './calendarAdapter';

export const startOfDay = defaultCalendarAdapter.startOfDay;
export const isSameDay = defaultCalendarAdapter.isSameDay;
export const isBeforeDay = defaultCalendarAdapter.isBeforeDay;
export const isBetweenDays = defaultCalendarAdapter.isBetweenDays;
export const formatInputDate = defaultCalendarAdapter.formatInputDate;
export const addDays = defaultCalendarAdapter.addDays;
